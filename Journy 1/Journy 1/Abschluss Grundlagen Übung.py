# 'Übung 1:'

'''number = 8 
result = ((number * 2) + 10 / 2)
print(int(result)) #gibt 16 aus, weil es als ganze Zahl interpretiert wird

print("Du hast die Zahl 6 ausgewählt" +
      " und das Ergebnis ist: " + str(result)) #gibt Du hast die Zahl 6 ausgewählt und das Ergebnis ist: 16 aus, weil es als String interpretiert wird

'''

# 'Übung 2:'

'''mail = "willy.wizard@zauberschule.de"
name = mail.split("@")[0] #gibt willy.wizard aus, weil es den Teil vor dem @-Zeichen nimmt
print(name) #gibt willy.wizard aus, weil es den Teil vor dem @-Zeichen nimmt

mail = "info@helena-hexe.com"
name = mail.split("@")[1]
name = name.split(".")[0]   #gibt helena-hexe aus, weil es den Teil vor dem .com nimmt
print(name) #gibt info aus, weil es den Teil vor dem @-Zeichen nimmt'''


# 'Übung 3:'

'''mail1 = "zarah.zauber@zauberberg.de"
mail2 = "info@trixie-trickser.com"
mail3 = "uwe_unhold@dunkelnetz.de" 

clients = [mail1, mail2, mail3] #Liste aller Studenten
print(clients)
print(len(clients)) #gibt die Anzahl an

zauberer = ["Buehnenzauberer", "magic.com"]
name = zauberer[0] + "@" + zauberer[1]
print(name)'''
